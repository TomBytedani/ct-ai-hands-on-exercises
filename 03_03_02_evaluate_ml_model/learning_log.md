# Lab 3.3.2 learning log

**Date:**

**Starting confidence (1–5):**

The notebook is the ordered learning path. Write here only when its matching
numbered checkpoint directs you to do so.

## Checkpoint 1 — Predict before model output

- **Q1.1:** My predicted effect of class imbalance on accuracy:
- **Q1.2:** Approximate accuracy of an always-`no` classifier and my calculation:
- **Q1.3:** Why that baseline can appear strong while never finding a subscriber:
- **Q1.4:** Comparison with the observed majority accuracy:

## Checkpoint 2 — Give each error a meaning

In this pre-call scenario, complete one short sentence for each:

- **Q2.1:** True positive, true negative, false positive, and false negative:
- **Q2.2:** The errors exposed by precision and recall:
- **Q2.3:** My Boolean-count expressions are in the notebook code cell.

## Checkpoint 3 — Reconstruct the metrics

Write each formula using `TP`, `TN`, `FP`, and `FN`, then state what its
denominator asks about:

- **Q3.1:** Accuracy, precision, recall, and F1 formulas:
- **Q3.2:** What each denominator asks about:
- **Q3.3:** My arithmetic expressions are in the notebook code cell.
- **Q3.4:** My match/mismatch prediction before scikit-learn verification:

## Checkpoint 4 — Interpret the evidence

- **Q4.1:** Metric/error-pattern comparison:
- **Q4.2:** Why accuracy alone is incomplete:
- **Q4.3:** Meaning of high precision with lower recall:
- **Q4.4:** Why model or threshold changes are outside this evaluation:
- **Q4.5:** Evidence that remains unknown while test is sealed:

## Retrieval after closing the notebook

1. **R1:** If `subscribed=1` is positive, what actual/predicted combination is an FN?
2. **R2:** Which denominator distinguishes precision from recall?
3. **R3:** Why is F1 called a harmonic balance of precision and recall?
4. **R4:** Can accuracy be high when a model predicts no positives? Under what data
   condition?
5. **R5:** Which dataset role did this lab use, and why did it not use the test set?

**Ending confidence (1–5):**

**One-sentence intuition to retain:**
